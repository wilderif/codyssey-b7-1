[project]
name = "codyssey-b7-1"
version = "0.1.0"
description = "FastAPI modular monolith chatbot"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "itsdangerous",
    "pydantic-settings",
    "uvicorn",
    "jinja2",
    "sqlalchemy",
    "openai",
    "python-multipart",
]

[dependency-groups]
dev = [
    "pytest",
    "pyright",
    "ruff",
    "httpx2>=2.9.1",
    "pytest-cov>=7.1.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

[tool.pyright]
venvPath = "."
venv = ".venv"
pythonVersion = "3.12"
strict = [
    "app/chat/context.py",
    "app/chat/openai_client.py",
    "app/chat/service.py",
    "tests/chat/test_context.py",
    "tests/chat/test_openai_client.py",
]
